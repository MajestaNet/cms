---
title: Objects
description: Static catalog of managed objects. Not GET /describe.
cmsPage: /objects
---

This is the public catalog of **managed** objects Majesta One ships. It is not authenticated `GET /client/v1/describe` and not `GET /metadata/v1/objects` — those reflect what this install has enabled and what the caller may see. Custom objects and fields stay on the install; they are not listed here.

Enable optional packs on [modules](/modules). Shape this install with [Metadata](/api/metadata).

---
title: CLI
description: one project and org validate or deploy.
cmsPage: /cli
---

The `one` CLI is how a customer repository is validated and promoted across that customer’s environments. Use it with MCP and family HTTP; do not fork the product image to customize an install.

Typical verbs: project/org `validate` and `deploy`. Details stay in the pinned product markdown below.
